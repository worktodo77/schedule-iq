# ScheduleIQ Metric & Heuristic Matrix

*Generated from `src/scheduleiq/metrics/matrix.yaml` by `scripts/render_matrix.py` — edit the YAML, not this file.*

This matrix is the authoritative inventory of every check ScheduleIQ runs.  The engine runs nothing that is not defined here, and reports anything defined but unimplemented as NOT EVALUATED.  Default thresholds are the published standard values; analysts may override them per run via a threshold profile, and every output records the threshold applied and its provenance.  Full citations for the reference keys are in [REFERENCES.md](REFERENCES.md).

**79 checks.**

## DCMA 14-Point Assessment

| ID | Check | Definition | Formula (as implemented) | Default threshold | Severity | References | Fuse equivalent |
|---|---|---|---|---|---|---|---|
| DCMA-01 | Logic (missing predecessors/successors) | Incomplete activities missing a predecessor and/or successor.  Open-ended activities float free of the network, so slippage does not propagate and the critical path is unreliable. | count(incomplete activities missing pred or succ) / count(incomplete activities), excluding LOE/summary | ≤ 5% | critical | DCMA14 Check 1 (<= 5%); GAO BP2 (sequencing; open ends); NASA STAT health check (<= 5%); PASEG §10.2 network logic | Missing Logic / Open Ends |
| DCMA-02 | Leads (negative lags) | Relationships carrying negative lag.  Leads distort the critical path, hide real work, cannot be statused, and invalidate risk simulation. | count(relationships with lag < 0, incomplete successor) / count(relationships with incomplete successor) | ≤ 0% | critical | DCMA14 Check 2 (zero leads); GAO BP2 (avoid leads); FUSE Number of Leads (0) | Number of Leads |
| DCMA-03 | Lags (positive lags) | Relationships carrying positive lag.  Lag is invisible unresourced 'work' that cannot be statused; excessive lag conceals scope and biases float. | count(relationships with lag > 0, incomplete successor) / count(relationships with incomplete successor) | ≤ 5% | warning | DCMA14 Check 3 (<= 5%); GAO BP2 (minimize/justify lags); NASA STAT (<= 5%) | Number of Lags |
| DCMA-04 | Relationship types (FS ratio) | Share of Finish-to-Start relationships.  FS logic is the most transparent; heavy SS/FF use obscures hand-offs and SF is effectively prohibited. | count(FS relationships) / count(all relationships), incomplete successors | ≥ 90% | warning | DCMA14 Check 4 (FS >= 90%); GAO BP2 (FS preferred, SF rare); PASEG §5 (relationships) | Relationship Types / FS Predecessor |
| DCMA-05 | Hard constraints | Incomplete activities with constraints preventing rightward movement (Must/Mandatory Start or Finish, Start/Finish On, Start/Finish No Later Than) — these override network logic and can fabricate or mask criticality. | count(incomplete with hard/late constraint) / count(incomplete) | ≤ 5% | critical | DCMA14 Check 5 (<= 5%); GAO BP2 (constraints minimized, justified, documented); SCL Protocol 2nd ed. (programme to be logic-driven); PASEG §5.5 (task constraints) | Hard Constraints |
| DCMA-06 | High float | Incomplete activities with total float greater than 44 working days (2 months).  Very high float usually means missing logic rather than genuine flexibility. | count(incomplete with TF > 44 wd via own calendar) / count(incomplete with float data) | ≤ 5% | warning | DCMA14 Check 6 (<= 5% over 44 wd); GAO BP7 (reasonable total float); NASA STAT (<= 5%) | High Float |
| DCMA-07 | Negative float | Incomplete activities with total float below zero.  The plan is not achievable as scheduled; requires a recovery plan or constraint correction.  For delay experts, negative float locates the contractual pressure points. | count(incomplete with TF < 0) / count(incomplete with float data) | ≤ 0% | critical | DCMA14 Check 7 (zero); GAO BP7 (negative float must be analyzed); NASA STAT (0) | Negative Float |
| DCMA-08 | High duration | Incomplete tasks with baseline (original) duration greater than 44 working days.  Long activities mask slippage and defeat progress measurement; decompose them. | count(incomplete tasks with OD > 44 wd via own calendar) / count(incomplete tasks) | ≤ 5% | warning | DCMA14 Check 8 (<= 5% over 44 wd); GAO BP4 (durations short and meaningful); FUSE Insufficient Detail | High Duration / Insufficient Detail |
| DCMA-09 | Invalid dates | Actual dates after the data date, or forecast dates before it.  Either corrupts every downstream calculation; a schedule failing this check cannot be relied on for analysis as-is. | count(actual start/finish > DD) + count(forecast start/finish < DD) | ≤ 0 | critical | DCMA14 Check 9 (zero); GAO BP9 (valid status); AACE29R §2.1 source validation (data date integrity); RPW Schedule Analyzer date checks | Invalid Dates / Invalid Forecast + Actual Dates |
| DCMA-10 | Resources (unloaded activities) | Incomplete tasks of at least one day with neither resources nor cost.  Applies when the schedule purports to be resource/cost loaded; unloaded work cannot support resource-based delay or disruption analysis. | count(incomplete tasks, dur >= 1d, no resources and no cost) / count(incomplete tasks, dur >= 1d) | ≤ 0% | warning | DCMA14 Check 10; GAO BP3 (resources on all activities) | Missing Resource Assignment |
| DCMA-11 | Missed tasks | Activities that finished, or are forecast to finish, later than their baseline finish among those due by the data date.  A leading indicator of baseline erosion. | count(due-by-DD activities with (actual or forecast finish) > baseline finish) / count(due-by-DD activities) | ≤ 5% | warning | DCMA14 Check 11 (<= 5%); PASEG execution metrics | Missed Tasks |
| DCMA-12 | Critical path test (continuity) | The critical path must run continuously from the data date to project completion, driven by logic rather than constraints.  DCMA's test perturbs a critical duration (+600d) and verifies 1:1 finish slip; without a scheduling engine ScheduleIQ verifies minimum-float path continuity and reports breaks for manual confirmation.  *Automated proxy; confirm with a +600d perturbation in P6/MSP when the finding matters.* | walk minimum-total-float activities; count members without an on-path successor (excluding project finish) | ≤ 0 | critical | DCMA14 Check 12; GAO BP6 (valid critical path); AACE78R (baseline CP review) | Critical Path Test |
| DCMA-13 | Critical Path Length Index (CPLI) | (Critical path length + total float to the contractual finish) / critical path length, in remaining working days.  Below 0.95 the remaining plan is inefficient against the commitment. | (CP length from DD to forecast finish + float to must-finish/baseline finish) / CP length | ≥ 0.95 | warning | DCMA14 Check 13 (>= 0.95); PASEG §10.4 (CPLI) | CPLI |
| DCMA-14 | Baseline Execution Index (BEI) | Activities completed divided by activities baselined to complete by the data date.  Below 0.95 the contractor is not executing to plan. | count(completed) / (count(baseline finish <= DD) + count(missing baseline dates))  — per PAM 200.1, sparse baselining penalized | ≥ 0.95 | warning | DCMA14 Check 14 (>= 0.95); PASEG §10.4 (BEI) | BEI / Baseline Execution Index |

## Logic & Network Quality

| ID | Check | Definition | Formula (as implemented) | Default threshold | Severity | References | Fuse equivalent |
|---|---|---|---|---|---|---|---|
| LOG-01 | Dangling starts (FF-only predecessors) | Activities whose only predecessors are FF (or SF): the start is logically undriven even though 'logic exists', so it passes the open-ends check while still floating free.  Unreliable for CPM and risk analysis. | count(incomplete activities whose predecessor set is all FF) | ≤ 0 | warning | GAO BP2 (dangling activities); FUSE Open Start (Dangling); RPW (hanging activities) | Open Start (Dangling Activity) |
| LOG-02 | Dangling finishes (SS-only successors) | Activities whose only successors are SS: the finish drives nothing downstream, so a growth in duration is invisible to the completion date. | count(incomplete activities whose successor set is all SS) | ≤ 0 | warning | GAO BP2 (dangling activities); FUSE Open Finish (Dangling) | Open Finish (Dangling Activity) |
| LOG-03 | Start-to-Finish relationships | SF logic is almost never a genuine plan of work and is effectively prohibited by every published standard. | count(SF relationships) | ≤ 0 | warning | DCMA14 Check 4 (SF ~ 0); GAO BP2 (SF strongly discouraged) | SF Predecessor |
| LOG-04 | Duplicate/parallel relationships | More than one relationship between the same activity pair (e.g., FS plus FF).  Sometimes legitimate, often accidental; inflates logic density and confuses tracing. | count(activity pairs with > 1 relationship) | ≤ 0 | info | FUSE Redundancy checks; RPW Schedule Analyzer | Redundant Logic (duplicate pairs) |
| LOG-05 | Logic density | Average relationships per activity.  Below ~2 the network is under-linked; above ~4 the logic is likely redundant or unmaintainable.  Informational band rather than pass/fail.  *Report flags < 2 and annotates > 4.* | count(relationships) / count(activities) | ≥ 2 | info | FUSE Logic Density (2-4 band); CIOB GPMT (network density guidance) | Logic Density |
| LOG-06 | Merge hotspots | Activities with an unusually high predecessor count.  Merge bias makes these dates optimistic (all predecessors must perform); prime candidates for risk focus.  ScheduleIQ flags >= 10 predecessors (Fuse's default trip of > 2 is noisy on construction schedules). | count(activities with >= 10 predecessors) | ≤ 0 | info | FUSE Merge Hotspot; GAO BP8 (merge bias in SRA) | Merge Hotspot |
| LOG-07 | Out-of-sequence progress | Successors started before their predecessors permitted.  OOS work means the as-built sequence departs from the logic; the P6 retained-logic vs progress-override setting then materially changes forecast dates — a core forensic intake question. | count(relationships where successor actual start precedes what the relationship allows) | ≤ 0 | warning | GAO BP9 (out-of-sequence corrected each update); PASEG §9.1 (statusing to timenow); RPW Inner Workings of P6 (retained logic vs progress override); AACE29R §2.2 update validation | Out of Sequence Progress |
| LOG-08 | Logic on LOE/summary activities | Relationships routed through level-of-effort, hammock, or summary activities.  Logic belongs on detail activities; LOE must never drive the critical path. | count(relationships with an LOE/summary endpoint) | ≤ 0 | warning | GAO BP2/BP6 (no summary logic; no LOE on CP); PASEG (LOE never drives discrete work) | Links to/from Summary Activities |
| LOG-09 | Redundant (transitive) logic | A->C when A->B->C already exists.  Redundant links inflate density and make every trace noisier; usually debris from schedule surgery. | count(FS relationships whose predecessor reaches the successor by another FS path) | ≤ 0 | info | FUSE Redundancy Index; RPW Schedule Analyzer (redundant relationships) | Redundancy Index Logic Check |
| REL-01 | Zombie relationships | Relationships whose predecessor or successor task ID does not resolve to an activity in the schedule.  A hallmark of export corruption or a partial activity delete that left dangling logic rows behind. | count(relationships where pred_uid or succ_uid not in schedule.activities) | ≤ 0 | warning | RPW Schedule Analyzer; AACE29R §2.1 (source validation) | — |

## Constraints

| ID | Check | Definition | Formula (as implemented) | Default threshold | Severity | References | Fuse equivalent |
|---|---|---|---|---|---|---|---|
| CON-01 | Soft constraints | One-way constraints (Start/Finish No Earlier Than, As Late As Possible).  Less damaging than hard constraints but still bend the network; each should be justified in the basis document. | count(incomplete with soft constraint) / count(incomplete) | ≤ 5% | info | GAO BP2 (all constraints minimized/justified/documented); FUSE Soft Constraints; AACE78R (constraint review) | Soft Constraints |
| CON-02 | Expected Finish constraints | P6 Expected Finish dates silently override remaining duration at each recalculation — a notorious source of 'durations that change by themselves' between updates. | count(incomplete activities with expected finish set) | ≤ 0 | warning | RPW Inner Workings of P6 (expected finish); AACE29R §2.2 (update validation) | — |
| CON-03 | As-Late-As-Possible activities | ALAP consumes all free float by design, pushing work (and cash) late and destroying early-warning float.  Rarely appropriate outside procurement/deliveries. | count(incomplete activities with ALAP constraint) | ≤ 0 | info | GAO BP2; RPW (ALAP cautions) | Soft Constraints (ALAP variant) |

## Float

| ID | Check | Definition | Formula (as implemented) | Default threshold | Severity | References | Fuse equivalent |
|---|---|---|---|---|---|---|---|
| FLT-01 | Critical percentage | Share of incomplete activities with total float <= 0.  A schedule that is 40% 'critical' has either real trouble or manufactured criticality (constraints, bad calendars); either way the critical path loses meaning. | count(incomplete with TF <= 0) / count(incomplete with float data) | ≤ 20% | info | FUSE Critical (informational); CIOB GPMT (criticality review) | Critical |
| FLT-02 | Near-critical percentage | Activities within 5 working days of critical.  A large near-critical population signals path volatility: the critical path can flip between updates, complicating delay attribution. | count(incomplete with 0 < TF <= 5 wd) / count(incomplete with float data) | informational | info | AACE29R (near-critical path identification, MIP support); FUSE Near-Critical | Near-Critical |
| FLT-03 | Float exceeding remaining project duration | Incomplete activities whose total float, in their own calendar's working days, exceeds the working-day-equivalent time remaining in the project.  Float cannot legitimately exceed the runway left to the forecast finish; when it does, the calendar or a constraint is producing an artifact rather than genuine flexibility. | count(incomplete activities with TF days (own calendar) > working-day-equivalent of (finish_date - data_date)), where working-day-equivalent = calendar_days * 5/7 + 2d buffer (approximation for holidays/rounding; does not model the project's actual holiday calendar) | ≤ 0 | warning | RPW Inner Workings of P6; GAO BP7 | — |

## Duration & Estimating

| ID | Check | Definition | Formula (as implemented) | Default threshold | Severity | References | Fuse equivalent |
|---|---|---|---|---|---|---|---|
| DUR-01 | Zero-duration tasks (non-milestones) | Tasks with zero duration that are not coded as milestones distort counts and usually indicate scope stubs or import artifacts. | count(incomplete tasks with OD = 0) | ≤ 0 | info | FUSE Zero Duration; RPW Schedule Analyzer | Zero Duration (non-milestone) |
| DUR-02 | Templated durations (multiples of 5 days) | A very high share of durations at 5/10/15/20 days suggests templated estimating rather than a basis of estimate — a credibility question for the baseline. | count(tasks with OD divisible by 5 wd) / count(tasks with OD > 0) | informational | info | GAO BP4 (durations from a documented basis); AACE78R (duration reasonableness) | — |
| DUR-03 | Original vs remaining duration mismatch (not started) | Not-started activities where remaining duration differs from original duration: someone edited RD (or an Expected Finish did) without progress — a silent replan. | count(not-started with |RD - OD| > 0) | ≤ 0 | warning | RPW Schedule Analyzer; AACE29R §2.2 (update validation) | — |

## Status & Date Integrity

| ID | Check | Definition | Formula (as implemented) | Default threshold | Severity | References | Fuse equivalent |
|---|---|---|---|---|---|---|---|
| DAT-01 | Status vs actual-date consistency | In-progress without an actual start, completed without actual dates.  Impossible states that break as-built extraction for delay analysis. | count(status/actual-date contradictions) | ≤ 0 | critical | AACE29R §2.2-2.3 (update and as-built validation); RPW Schedule Analyzer; GAO BP9 | Percent Complete anomalies |
| DAT-02 | Actual dates without progress | Not-started activities carrying actual dates — statusing errors or import damage. | count(not-started with actual start or finish) | ≤ 0 | warning | RPW Schedule Analyzer; GAO BP9 | — |
| DAT-03 | Suspend/resume usage | Suspended activities stop consuming remaining duration invisibly; each suspension needs a documented reason and matters for disruption analysis. | count(activities with suspend or resume dates) | informational | info | RPW Inner Workings of P6 | — |
| DAT-04 | Data date currency | Days between the data date and the file's export date.  A schedule exported long after its data date may have been reworked post-period — a chain-of-custody flag for expert use. | abs(export date - data date), days | ≤ 45 | info | AACE29R §2.1 (source validation); SCL Protocol (contemporaneous records, Core Principle 1) | — |
| DAT-05 | Actual-duration anomalies | Actual finish before actual start, actuals reported earlier than the project start date, or a completed task with positive original duration whose actual start and finish coincide (zero as-built span) — as-built corruption that must be resolved before the dates support delay analysis. | count(actual finish < actual start) + count(actual start/finish < project start_date) + count(completed tasks with OD > 0 and actual_finish == actual_start) | ≤ 0 | critical | AACE29R §2.3 (as-built validation); RPW Schedule Analyzer | — |

## Calendars

| ID | Check | Definition | Formula (as implemented) | Default threshold | Severity | References | Fuse equivalent |
|---|---|---|---|---|---|---|---|
| CAL-01 | Calendars in use | Inventory of calendars actually assigned.  Many calendars (or surprise resource calendars) are the leading cause of float values analysts cannot reproduce. | count(distinct calendars assigned to activities) | informational | info | RPW Inner Workings of P6 (calendars & float); AACE29R §2.1 | Number of Calendars |
| CAL-02 | Non-standard hours-per-day calendars | Calendars not using 8h days make 'days' displays misleading (a 10-day duration on a 12h calendar is not what it looks like) and distort every day-denominated metric. | count(activities on calendars with hours/day != 8) | informational | info | RPW Inner Workings of P6 (hours-per-day distortion) | — |
| CAL-03 | Working calendars without holidays | A 5-day calendar with zero holiday exceptions almost certainly overstates capacity; check against the project location's calendar. | count(assigned 5-day calendars with no non-work exceptions) | ≤ 0 | info | CIOB GPMT (calendar realism); AACE78R (calendar review) | — |
| CAL-05 | Quantified multi-calendar float distortion at the driving path | For each activity on the driving path (reused from analytics.paths.driving_path, never recomputed), total float is restated in days via the activity's own calendar hours/day and, separately, via the project's dominant calendar hours/day; the two figures diverge whenever a driving-path activity sits on a non-dominant-hours calendar, because a day of float is not a calendar-neutral unit. | for each driving-path step with total_float_hours: own_days = TF_hr / own_calendar.hours_per_day; dominant_days = TF_hr / dominant_calendar.hours_per_day (dominant = calendar covering the most real activities); report max(abs(own_days - dominant_days)) across the path, with per-activity findings where the divergence exceeds 0.5d | informational | info | RPW Inner Workings of P6 (multi-calendar float); AACE29R (critical path identification) | — |

## Resources & Cost

| ID | Check | Definition | Formula (as implemented) | Default threshold | Severity | References | Fuse equivalent |
|---|---|---|---|---|---|---|---|
| RES-01 | Milestones carrying resources | Milestones with resource assignments distort resource and EV metrics (and in Fuse's DCMA-with-EV variant require special handling). | count(milestones with assignments) | ≤ 0 | info | PASEG (milestones carry no work) | — |
| RES-02 | Completed activities with remaining values | Completed work retaining remaining duration, units, or cost — corrupt statusing that inflates the remaining plan. | count(completed with RD > 0 or remaining units/cost > 0) | ≤ 0 | warning | RPW Schedule Analyzer; PASEG (statusing hygiene) | — |

## Structure & Critical Path

| ID | Check | Definition | Formula (as implemented) | Default threshold | Severity | References | Fuse equivalent |
|---|---|---|---|---|---|---|---|
| CP-01 | Calendars on the critical path | Multiple calendars along the critical path make total float non-uniform (float is calendar-denominated), so 'zero float' means different things on different activities — confirm criticality by longest path, not float alone. | count(distinct calendars among TF <= 0 activities) | informational | info | RPW Inner Workings of P6 (multi-calendar float); AACE29R (critical path identification) | — |
| STR-01 | Schedule census | Activity/relationship/WBS counts and type distribution — the basic profile every intake memo starts with. | counts by type | informational | info | FUSE Characteristics group; GAO BP1 (capture all activities) | Characteristics (Milestones/Normal/Summaries/Hammocks) |
| STR-02 | Start and finish milestones present | The project should open and close on milestones so the network has defined entry/exit points. | count(start milestones) + count(finish milestones) | ≥ 2 | info | PASEG (program start/finish milestones); CIOB GPMT | Milestones |
| PRG-01 | Progress census | Completed / in-progress / not-started distribution — context for every other update-file metric. | status distribution over real activities | informational | info | FUSE Progress/Status group | In Progress / Complete / Not Started |

## Trend & Change (series)

| ID | Check | Definition | Formula (as implemented) | Default threshold | Severity | References | Fuse equivalent |
|---|---|---|---|---|---|---|---|
| TRD-01 | Float erosion trend | Total float consumed per period on key paths.  Sustained erosion without recovery action is the classic early-warning the tribunal will ask why nobody saw. | delta of mean/min total float between consecutive updates | informational | info | PASEG §10.4 (float trend / TFCI); SCL Protocol (contemporaneous monitoring) | Performance trending |
| TRD-02 | Critical path stability | Overlap (Jaccard) of critical-set membership between consecutive updates.  A path that flips every month makes windows-based delay methods hard and points at logic churn or calendar noise. | |CP(n) intersect CP(n+1)| / |CP(n) union CP(n+1)| | informational | info | AACE29R (MIP selection: path volatility) | — |
| TRD-03 | Logic churn rate | Relationships added plus deleted between updates, as a share of the network.  High churn immediately before a claim window is a red flag for retrospective logic engineering. | (rels added + rels deleted) / rels in earlier update | informational | info | AACE29R §2.2 (update validation: logic changes); FUSE Forensics (logic changes) | Forensic Analyzer logic changes |
| TRD-04 | Forecast completion slippage | Movement of the forecast project finish per update, against data-date progression: slip rate > 1 means the end date moves faster than time passes. | delta(forecast finish) / delta(data date) per period | informational | info | PASEG §10.4 (completion trend / schedule rate chart); SCL Protocol Pt B (progress records) | Performance trending |
| TRD-05 | Retroactive actual-date changes | Actual dates reported in one update and changed in a later one — 'rewriting history'.  Central to as-built reliability; every instance must be explained before the schedule is used for delay analysis. | count(activities whose actual start/finish differs between updates where both reported) | ≤ 0 | critical | AACE29R §2.3 (as-built validation); RPW Schedule Analyzer (actual date changes) | Forensic Analyzer progress changes |
| TRD-06 | Duration changes to existing activities | Original-duration edits between updates (especially on in-progress or completed work) — silent replans that mask slippage. | count(activities with OD changed between updates) | informational | info | AACE29R §2.2; FUSE Forensics (duration updates) | Forensic Analyzer duration changes |
| TRD-07 | Scope churn (activities added/deleted) | Activities added or removed per update.  Tracks scope evolution and locates when claim-relevant work entered the schedule. | count(added) + count(deleted) per update pair | informational | info | GAO BP10 (baseline change control); FUSE Forensics (added/deleted) | Forensic Analyzer added/deleted activities |
| EVM-01 | Hit Task Percentage | Share of tasks baselined to finish in the period that actually finished on or before their baseline finish.  Read alongside BEI: BEI is cumulative, Hit Task % is per-period execution discipline. | count(finished on/before baseline finish, within period) / count(baseline finish within period) | ≥ 80% | info | DCMA-EA PAM 200.1 (schedule execution metrics); PASEG §10.4 | Hit Task % |
| EVM-02 | Current Execution Index (CEI) | Of tasks forecast (at the start of the period) to finish within the period, the share that actually finished.  Measures forecast credibility — a schedule whose near-term forecasts keep missing cannot be relied on for look-ahead analysis. | count(actually finished in period, of those forecast to) / count(forecast to finish in period) | ≥ 0.75 | info | DCMA-EA PAM 200.1 (schedule execution metrics); PASEG §10.4.5 (CEI) | — |
| TRD-08 | Baseline/plan date changes | Planned (target) dates shifted between updates without a formal re-baseline — erodes the yardstick that variances are measured against. | count(activities with planned start/finish changed between updates) | informational | info | GAO BP10 (maintain baseline); SCL Protocol (programme updating discipline) | Forensic Analyzer date changes |
| SET-01 | Scheduling-settings drift between updates | Changes to core scheduling settings (retained logic vs progress override, relationship lag calendar, critical float threshold, open-ends criticality, use of Expected Finish) between consecutive updates.  A settings flip silently changes every forecast date without touching a single activity — 'recovery by settings' rather than by performance. | count(Schedule.settings fields changed between consecutive updates: retained_logic, progress_override, relationship_lag_calendar, critical_float_threshold_hours, make_open_ends_critical, use_expected_finish); each changed field is one finding naming before/after | ≤ 0 | critical | RPW Inner Workings of P6 (settings); AACE29R §2.2 (update validation) | — |
| SET-02 | Engine match rate (validation handshake) | The ADR-0007 diagnostic CPM engine re-schedules the file exactly as imported (actual-date-anchored, honoring its date constraints and scheduling options) and its computed dates/floats are compared to the tool-of-record values stored in the file.  The match rate is reported; below the threshold, engine-dependent features refuse to run and the mismatches are listed.  Diagnostic only: it measures the ported engine's fidelity to the schedule of record, not a defect in the schedule itself.  *Tool-of-record dates remain the schedule; engine output is diagnostic only (ADR-0007).  Refusal gate for engine-dependent features.* | population = real activities present in both the engine result and the record; match = an activity whose every compared field is within tolerance (CALENDAR_AWARE: dates within +-1 calendar day — LIM-044 carried; floats within +-1 workday); value = 100 x matched / population.  When the network fails the engine's validator, the handshake is a fail with value 0 and the blocking issues listed. | ≥ 99% | info | AACE29R §2.1-2.3 (source validation discipline); SCL Protocol 2nd ed. (reliance on the schedule of record) | — |
| CAL-04 | Calendar definition changes between updates | Edits to a calendar's own definition (hours per day, workdays per week, weekly work-pattern spans, or holiday exceptions) between updates, for calendars present in both.  Editing the calendar re-times every activity assigned to it without changing a single duration or date field — a near-invisible way to move dates. | for calendars matched by uid (or by name when uid changes), diff hours_per_day, workdays_per_week, work_patterns spans, and exceptions_nonwork (holidays added/removed); each changed field is one finding | informational | info | RPW Inner Workings of P6 (calendars & float); AACE29R §2.2 | — |
| LOG-10 | Hollow-logic screen | Activities added between updates with original duration of one day or less that already carry at least one predecessor and one successor in the later schedule — inserted directly into an existing logic chain rather than representing new discrete scope.  A classic path-rerouting stub ahead of a claim window. | count(ChangeSet.added activities with original duration <= 1 working day that have >= 1 predecessor and >= 1 successor among the later schedule's relationships) | ≤ 0 | info | AACE29R §2.2 (update validation: logic changes) | — |
| DUR-04 | Remaining-duration compression without progress | Remaining duration falling faster than physics or reported progress justifies, for activities not yet complete in the later update — schedule crashing on paper to hold a date rather than genuine progress.  Branch 1 applies to every activity: RD cannot legitimately drop by more than the working time that passed between the two data dates on the activity's own calendar; exceeding it is a silent re-estimate.  Branch 2 compares RD movement to reported percent movement, and applies ONLY to Physical/Units percent-complete activities: Duration-% activities are excluded because P6 derives their percent from RD/OD, which would make the comparison self-referential. | branch 1 (all percent types): count(activities in both updates, not completed later, where (rd_earlier - rd_later) > working hours available between the data dates on the activity's own calendar (Calendar.is_workday x hours_per_day, iteration capped at 5 years) + 8h tolerance); branch 2 (Physical/Units percent only): count(rd_later < rd_earlier * (1 - max(0, pct_later - pct_earlier) / 100) - 4h tolerance); findings name the branch tripped, RD before/after, and (branch 2) pct before/after | ≤ 0 | warning | RPW Schedule Analyzer; PASEG §9 (statusing discipline) | — |
| STR-03 | WBS re-parenting churn | Activities present in both updates whose resolved WBS node (code/name, not raw uid) changed — scope re-mapping that breaks the vertical traceability an expert relies on to tie activities back to the contract/cost breakdown. | count(activities matched by code across a consecutive pair whose earlier.wbs[wbs_uid] code/name differs from later.wbs[wbs_uid] code/name) | informational | info | GAO BP5 (vertical traceability) | — |

## LI Proprietary Indices

| ID | Check | Definition | Formula (as implemented) | Default threshold | Severity | References | Fuse equivalent |
|---|---|---|---|---|---|---|---|
| LI-01 | FCBI — Float Criticality Burn Index | Float consumption weighted by path criticality (kernel 2^(-RF/lambda), lambda 5 wd).  Burn and recovery tracked separately; normalized burn above 100% means erosion beyond available float (paths driven deeper negative).  RJL concept. | sum over activities of max(0, -dTF days) x 2^(-RF/5); RF = min relative float of containing float paths | informational | info | LI bespoke methodology — ANALYTICS_PROPOSAL.md §9.1 | — |
| LI-02 | LHL — Logic Half-Life | Kaplan-Meier median survival of relationship signatures across updates, in months; on/off driving-path split.  Short half-life = the network is a rolling narrative, not a plan. | KM median of (pred, succ, type) lifespans, right-censored; baseline pair excluded by default | informational | info | LI bespoke methodology — ANALYTICS_PROPOSAL.md §9.2 | — |
| LI-03 | FRB — Forecast Reliability Band | Empirical error band on the scheduler's own forecasts: central-80% interval of (actual - forecast finish) working days, bucketed by forecast horizon; bias and dispersion reported separately. | per horizon bucket: median, P10, P90 of realized forecast errors | informational | info | LI bespoke methodology — ANALYTICS_PROPOSAL.md §9.3 | — |
| LI-04 | PCI — Path Concentration Index | Herfindahl concentration of criticality across the top float paths (same kernel as FCBI).  Falling PCI is a leading indicator of critical-path instability; a sudden rise alongside logic churn suggests engineered consolidation. | sum of squared kernel-weight shares over top-10 float paths, per update | informational | info | LI bespoke methodology — ANALYTICS_PROPOSAL.md §9.4 | — |
| LI-05 | RDI — Recovery Debt Index | Cumulative days of completion forecast resting on never-demonstrated pace: accrues when required near-critical pace exceeds the maximum demonstrated pace. | sum over windows of max(0, required pace - max demonstrated pace) x window working days | informational | info | LI bespoke methodology — ANALYTICS_PROPOSAL.md §9.5 | — |
| LI-06 | BDI — Baseline Dilution Index | Share of the latest driving path (by working-day length) attributable to post-baseline activities or logic — how much of today's completion story was never in the contract baseline. | 100 x post-baseline step-days / total step-days along the latest driving path | informational | info | LI bespoke methodology — ANALYTICS_PROPOSAL.md §10.1 | — |
| LI-07 | CDI — Criticality Dwell Index | Each activity's share of total project criticality-time (one unit allocated per update across the near-critical band by kernel weight).  The leaderboard is the delay analysis's cast of characters. | accumulated kernel-weighted allocation per activity / windows allocated; scalar = top-decile share | informational | info | LI bespoke methodology — ANALYTICS_PROPOSAL.md §10.2 | — |
| LI-08 | IL — Intervention Latency | Median updates (and days) from a chain first showing negative float to the first responsive edit on that chain (logic, duration, calendar, or resources).  Quantifies mitigation responsiveness; distinguishes 'did not act' from 'acted and it did not work'. | median over emergence events of (first responsive update - emergence update); unresolved counted separately | informational | info | LI bespoke methodology — ANALYTICS_PROPOSAL.md §10.3 | — |
| LI-09 | BWI — Bow-Wave Index | Remaining near-critical work per remaining working period ahead of a held milestone, normalized to the first update.  Rising BWI while the date holds = compression that must eventually break; projected break reported against demonstrated pace. | density_u / density_first, density = near-critical remaining days / working days to target | informational | info | LI bespoke methodology — ANALYTICS_PROPOSAL.md §10.4 | — |
| LI-10 | MML — Measured-Mile Locator | Per trade/WBS: disruption contrast ratio (most-impacted window productivity / cleanest window productivity) with candidate periods located; flags when no clean mile exists.  Preliminary — period selection confirmed by the expert. | min-window productivity / max-window productivity per top-level WBS; resource-based where loaded, activity-day basis otherwise | informational | info | LI bespoke methodology — ANALYTICS_PROPOSAL.md §10.5 | — |

## LI Proprietary Indices (provocative, internal)

| ID | Check | Definition | Formula (as implemented) | Default threshold | Severity | References | Fuse equivalent |
|---|---|---|---|---|---|---|---|
| LI-11 | SMI — Schedule Manipulation Indicator | Composite triage indicator of the independent curation signals already computed — retroactive actuals (TRD-05), hollow logic (LOG-10), settings flips (SET-01), calendar-definition edits (CAL-04), statistical anomalies (Benford/round-number/percent-step), silent replans (DUR-03/-04), and churn concentrated before claim submissions — each scored for presence, severity, and timing on a published weighting.  Reported only with its full decomposition and the innocent explanations for every contributing signal; internally a triage dial (15 = run the standard checks; 80 = budget for forensics).  *Privileged/internal triage indicator — an observation consistent with schedule curation that warrants explanation, never a finding of intent; not part of the standard report; every contributing signal carries its innocent explanations in the output; language capped at 'warrants explanation'.* | SMI = 100 x Σ(w_i · signal_i) / Σ w_i over six signals with published weights TRD-05 3, SET-01 3, LOG-10 2, DUR-03/-04 2, CAL-04 1, statistical-anomalies 1; each signal_i (0-100) = 0.30·presence + 0.50·severity + 0.20·timing, severity = 100·min(1, count/5), timing = 100 when the signal's edits fall within 60 days before a claim date (else the timing sub-weight is dropped and presence/severity are renormalized, disclosed) | informational | info | LI bespoke methodology — ANALYTICS_PROPOSAL.md §11.1; AACE29R §2.2-2.3 (update/as-built validation); SCL Protocol 2nd ed. (contemporaneous records) | — |
| LI-12 | DDI — Directed Date Index | Detects the signature of a dictated completion date: the selected milestone holds station update after update while the schedule's own fundamentals deteriorate (RDI climbing, BWI climbing, criticality-weighted float burn, constraint additions near the target, uncompensated duration compressions).  Claims only inconsistency between the held date and the fundamentals; why the date was held is expressly left open (owner pressure, genuine recovery intent, or negotiation posture).  *Privileged/internal triage indicator — the finding is 'the forecast completion is increasingly unsupported by the schedule's internal indicators' (defensible arithmetic), never a finding of intent; why the date was held is left to the expert; not part of the standard report; capped at 'warrants explanation'.* | DDI = held_updates × mean deterioration z-score of the fundamentals over the held span; held_updates = length of the trailing run of updates whose milestone finish held within ±1 day; fundamentals (RDI cumulative, BWI, FCBI cumulative, near-target late-constraint count, cumulative DUR-04 compressions) are population z-scored across the series (higher = worse) and averaged per update; NOT COMPUTABLE for < 3 updates or when the date did not hold ≥ 2 consecutive updates | informational | info | LI bespoke methodology — ANALYTICS_PROPOSAL.md §11.2; AACE29R §2.2 (update validation) | — |
| LI-13 | ARR — Attribution Robustness Ratio | Quantifies how method-dependent the delay case is: each party's attributed delay is expressed as a share across every defensible N4 method variant (window boundaries, statusing, MIP 3.3 vs 3.4, contested-revision treatment), and ARR is the narrowest-to-widest ratio.  ARR near 1.0 = a method-independent conclusion; ARR of 0.3 = the answer is mostly methodology.  Generated as privileged work product; knowing a conclusion's robustness is required diligence.  *Privileged/internal work product by default — a robustness screen of the METHOD, not an apportionment (apportionment is reserved to the expert); not part of the standard report; capped at 'warrants explanation'.* | per party, ARR = min share ÷ max share of attributed delay across the computable N4 robustness variants; share = party total ÷ all-party total per variant (zero-denominator variants skipped); parties with zero attribution in every variant are excluded; requires an N4 RobustnessCertificate with a responsibility overlay, else NOT COMPUTABLE | informational | info | LI bespoke methodology — ANALYTICS_PROPOSAL.md §11.3 / §8.4 (N4 sweep); AACE29R (MIP selection; method sensitivity); SCL Protocol 2nd ed. | — |
| LI-14 | PPS — Pacing Plausibility Score | The pacing defense, scored instead of argued.  For each §6.3 pacing candidate (non-critical deceleration concurrent with a parent critical delay) the recognized pacing criteria are scored on a published weighting.  The score triages which pacing assertions deserve expert investment; the same table printed for an opposing expert's pacing claim is a ready-made cross-examination.  Symmetric by construction — our clients' pacing claims are graded too.  *Privileged/internal triage indicator — preliminary; the pacing defense turns on contemporaneous intent, reserved to the expert; not part of the standard report; capped at 'warrants explanation'.* | PPS = Σ(criterion_i · weight_i), 0-100 per pacing candidate, published weights: float-existed-at-start 0.25, contemporaneous-awareness 0.20, proportionality-to-float 0.20, reversibility 0.20, re-acceleration-after-parent-resolution 0.15; each criterion scored 0/100 on its evidence, and missing-evidence criteria score the neutral placeholder 50 (never zero) with disclosure | informational | info | LI bespoke methodology — ANALYTICS_PROPOSAL.md §11.4 / §6.3; SCL Protocol 2nd ed. (pacing / concurrency); AACE29R (pacing criteria) | — |
| LI-15 | RSA — Rebuttal Surface Area | An adversarial audit of a delay analysis — ours or theirs.  The attributed delay is decomposed by window and each component classified method-sensitive (the N4 sweep changes it), data-fragile (rests on records failing integrity checks — retroactive actuals, as-built anomalies, silent compression), path-ambiguous (low PCI in the window), or robust.  Run on our own draft it hardens the report before signature; run on the opposing analysis it is a cross-examination map.  *Privileged/internal work product by default — symmetric by construction (the same arithmetic applies to both sides' work); not part of the standard report; capped at 'warrants explanation'.* | RSA = 100 × Σ delay(contested windows) ÷ Σ delay(all attributed windows), where contested = method-sensitive (window total moves > 2 wd across the N4 variants) ∪ data-fragile (window overlaps TRD-05/DAT-05/DUR-04 findings) ∪ path-ambiguous (window PCI < 0.20); each component named and its classification evidence cited; requires an N4 RobustnessCertificate with a responsibility overlay, else NOT COMPUTABLE | informational | info | LI bespoke methodology — ANALYTICS_PROPOSAL.md §11.5 / §8.4 (N4 sweep); AACE29R (source validation; method sensitivity); SCL Protocol 2nd ed. | — |

